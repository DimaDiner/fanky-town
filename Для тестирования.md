Без проблем! После перезагрузки компьютера тебе нужно запустить три вещи в строгом порядке. 

Главное правило: **бесплатный Ngrok каждый раз выдает новую ссылку**. Поэтому нам придется обновить её в коде.

Вот пошаговая инструкция для запуска:

### 1️⃣ Шаг 1: Запускаем туннель (Ngrok)
Нам нужен новый адрес для интернета.
1. Открой программу **ngrok** (черное окошко), которую ты скачивал вчера.
2. Напиши команду:
   ```cmd
   ngrok http 8000
   ```
3. Скопируй новую ссылку `https://.......ngrok-free.app` (строка Forwarding). **Окно ngrok не закрывай.**

### 2️⃣ Шаг 2: Обновляем ссылку в проекте
Телеграм-бот должен узнать твой новый адрес.
1. Открой проект в VS Code.
2. Открой файл **`config.py`**.
3. Замени значение `WEBAPP_URL` на твою новую ссылку из ngrok:
   ```python
   WEBAPP_URL = "https://твоя-новая-ссылка.ngrok-free.app" 
   ```
4. **Обязательно сохрани файл** (`Ctrl+S`).

### 3️⃣ Шаг 3: Запускаем Сервер (Терминал 1)
1. В VS Code открой терминал (`Terminal -> New Terminal`).
2. Активируй виртуальное окружение:
   ```cmd
   .\venv\Scripts\activate
   ```
   *(Убедись, что появилась зеленая `(venv)`)*.
3. Запусти бэкенд:
   ```cmd
   uvicorn main:app --reload
   ```
   *(Дождись надписи `Application startup complete`)*.

### 4️⃣ Шаг 4: Запускаем Бота (Терминал 2)
1. Открой **второй** терминал в VS Code (нажми на плюсик `+` справа в панели терминалов).
2. Снова активируй окружение во втором окне:
   ```cmd
   .\venv\Scripts\activate
   ```
3. Запусти бота:
   ```cmd
   python bot/run_bot.py
   ```
   *(Дождись надписи `🤖 Бот запущен...`)*.

---

### 🚀 Всё готово! Как тестировать то, что мы починили:

1. Зайди в бота в Телеграме с телефона.
2. Напиши `/start` (чтобы бот обновил кнопку с новой ссылкой ngrok).
3. Нажми **"📝 Регистрация"**.
4. ⚠️ **Важно:** Когда анкета откроется, нажми на 3 точки в правом верхнем углу и выбери **"Обновить страницу"** (чтобы телефон скачал наш новый исправленный код).
5. Введи выдуманный номер (например, `7770001122`) и нажми "Зарегистрироваться".
6. После успешной регистрации нажми в боте кнопку **"💰 Мой баланс"**. 

Если бот ответит *"💳 Ваш баланс: 0 бонусов"* — значит, наш фикс сработал, и Телеграм успешно передал твой реальный ID в базу данных! Жду результатов.


адрес для админки: http://127.0.0.1:8000/admin/